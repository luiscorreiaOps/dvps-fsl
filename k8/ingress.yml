apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: fsl-challenge-ingress
  namespace: production
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
  - host: fsl-challenge.me
    http:
      paths:
      - path: /testpath
        pathType: Prefix
        backend:
          service:
            name: fsl-challenge-service
            port:
              number: 8080
